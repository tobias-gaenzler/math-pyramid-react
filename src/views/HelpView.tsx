import { Container } from "@mui/material";

function HelpView() {
  return (
    <Container
      sx={{
        height: "100%",
        display: "grid",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <div>Help View</div>
    </Container>
  );
}

export default HelpView;
